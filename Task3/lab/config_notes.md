# Configuration Notes

* Sudo version 1.8.31 is installed in `/usr/local/bin/sudo`.
* The default user is `tester` with the password `password`.
* The `tester` user is not in the sudoers file. This matches the real world conditions for this privilege escalation vulnerability.
* No extra dependencies are installed beyond Python 3 and basic build tools. Ask Member 1 if you need specific debugging tools added to the Dockerfile.
